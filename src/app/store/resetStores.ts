import { createAction } from '@reduxjs/toolkit'

/**
 * Сигнал «сбросить пользовательские данные»: все data-слайсы возвращаются к
 * initialState. Диспатчится при смене аккаунта / входе / выходе, чтобы в UI не
 * мелькали данные предыдущего пользователя, пока грузятся новые.
 *
 * Слайс `auth` намеренно сохраняется (его обновляют bootstrap/signOut),
 * иначе на миг показывался бы залогаут при переключении аккаунта.
 */
export const resetStores = createAction('app/resetStores')
