-- ============================================================
-- 0042_vacancy_schedule.sql
-- График работы вакансии (классический: 5/2, 2/2, 6/1, сменный, гибкий, свободный).
-- Хранится как текст (значение из WorkSchedule на фронте). Может быть NULL —
-- старые вакансии без графика просто не попадают под фильтр «График работы».
-- Применять ПОСЛЕ 0041. Supabase Dashboard → SQL Editor → вставить → Run.
-- ============================================================

alter table public.vacancies
  add column if not exists schedule text;
